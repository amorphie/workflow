apiVersion: v1
kind: Service
metadata:
  name: amorphie-workflow-hzconsumer
  namespace: amorphie-workflow-hzconsumer
spec:
  selector: 
    app: amorphie-workflow-hzconsumer
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000 